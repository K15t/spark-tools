    <!-- added by spark-tools -->
    <servlet name="{{spa.name}} SPARK App Servlet" key="spark-{{spa.key}}-servlet" class="com.k15t.spark.jira.JiraAppServlet">
        <description>Outputs the resources for the {{spa.name}} SPARK app.</description>
        <url-pattern>/{{spa.key}}*</url-pattern>
        <init-param>
            <param-name>resource-path</param-name>
            <param-value>/spark/{{spa.key}}</param-value>
        </init-param>
    </servlet>
{{#isAdminApp}}
    <web-item name="{{spa.name}} SPARK App Web-Item" key="spark-{{spa.key}}-webitem" section="system.admin.header.pageactions" weight="10">
        <description>Adds a menu entry for the {{spa.name}} SPARK app to the admin console.</description>

        <label>{{spa.name}}</label>
        <link linkId="spark-{{spa.key}}-webitem">/plugins/servlet/{{spa.key}}</link>
    </web-item>
{{/isAdminApp}}
{{#isDialogApp}}

    <web-resource name="{{spa.name}} SPARK App Bootstrap Resources" key="spark-{{spa.key}}-web-resource">
        <description>Bootstraps the {{spa.name}} SPARK app.</description>

        <context>page</context>
        <context>blogpost</context>

        <resource type="download" name="lazyload.js" location="com/k15t/spark/jira/{{spa.template.framework}}/lazyload.js"/>
        <resource type="download" name="dialog-app.css" location="com/k15t/spark/jira/{{spa.template.framework}}/dialog-app.css"/>
        <resource type="download" name="spark-dialog-app-bootstrap.soy.js" location="com/k15t/spark/jira/{{spa.template.framework}}/dialog-app-bootstrap.soy"/>
        <resource type="download" name="spark-dialog-app-bootstrap.js" location="com/k15t/spark/jira/{{spa.template.framework}}/dialog-app-bootstrap.js"/>
        <resource type="download" name="dialog-app.js" location="spark/{{spa.key}}/dialog-bootstrap.js"/>

        <transformation extension="soy">
            <transformer key="soyTransformer"/>
        </transformation>
    </web-resource>

    <web-item name="{{spa.name}} SPARK App Web-Item" key="spark-{{spa.key}}-webitem" section="browse_link/project_new" weight="200">
        <description>Adds a menu entry for the {{spa.name}} SPARK app to the tools menu.</description>

        <label>{{spa.name}}</label>
        <link linkId="spark-{{spa.key}}-webitem"></link>
    </web-item>
{{/isDialogApp}}
{{#isSpaceApp}}

    <webwork1 key="spark-{{spa.key}}-actions" name="{{spa.name}} SPARK App Action">
        <description>{{spa.name}} SPARK App Action</description>
        <actions>
            <action name="spark.GENERATED.{{spa.keyForJavaPackage}}.GeneratedJiraAppAction" alias="index">
                <view name="success">/com/k15t/spark/jira/space-app.vm</view>
                <!--<result name="notpermitted" type="redirect">/pages/pagenotpermitted.action</result>-->
                <!--<param name="resource-path">/spark/{{spa.key}}</param>-->
            </action>
        </actions>
    </webwork1>

    <web-item name="{{spa.name}} SPARK Web-Item" key="spark-{{spa.key}}-webitem" section="system.admin.header.pageactions" weight="10">
        <description>Adds a tab entry for the {{spa.name}} SPARK app within the Space Tools / Add-ons section.</description>
        <label>{{spa.name}}</label>
        <link id="spark-{{spa.key}}-webitem">/secure/{{spa.key}}/index.jspa</link>
    </web-item>
{{/isSpaceApp}}
    <!-- /added by spark-tools -->
